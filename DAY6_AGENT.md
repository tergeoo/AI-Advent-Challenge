# День 6: Первый AI Агент

## Описание

Это полноценный AI агент с CLI интерфейсом, реализованный как отдельная сущность. Агент запоминает контекст разговора и может поддерживать длительный диалог.

## Архитектура

### Компоненты

```
cmd/advent/day6/main.go           - CLI интерфейс и точка входа
internal/agent/agent.go           - Агент как отдельная сущность
```

### Ключевые концепции

**1. Агент как отдельная сущность**

Агент - это не просто обертка над API запросом. Это полноценный объект с:
- Собственным состоянием (история диалога)
- Конфигурацией (модель, температура, системный промпт)
- Методами для взаимодействия (Ask, ClearHistory, GetHistory)

**2. Инкапсуляция**

Вся логика работы с LLM инкапсулирована внутри агента:
- Управление историей диалога
- Формирование сообщений для API
- Обработка ответов
- Подсчет токенов

**3. Память диалога**

Агент автоматически сохраняет историю:
```go
type Message struct {
    Role      string    // "user" или "assistant"
    Content   string    // Содержание
    Timestamp time.Time // Когда отправлено
}
```

## Использование

### Запуск

```bash
make day6
# или
go run cmd/advent/day6/main.go
```

### Интерактивный режим

После запуска вы попадаете в интерактивный режим:

```
================================================================================
Day 6: Первый AI Агент
================================================================================

🤖 О АГЕНТЕ
--------------------------------------------------------------------------------
Это простой AI агент с памятью диалога.

Возможности агента:
  • Запоминает контекст разговора
  • Отвечает на вопросы
  • Помогает с задачами
  • Поддерживает команды для управления

Доступные команды:
  /help     - показать справку
  /history  - показать историю диалога
  /clear    - очистить историю
  /stats    - показать статистику
  /exit     - выйти из программы

💬 Вы: _
```

### Примеры использования

#### Пример 1: Простой вопрос

```
💬 Вы: Привет! Как тебя зовут?

🤖 Агент: Привет! Я AI ассистент, созданный для помощи. У меня нет имени, 
но вы можете называть меня как вам удобно. Чем могу помочь?

├─ Токены: 87
├─ Время: 1.2s
└─ Сообщений в истории: 2
```

#### Пример 2: Диалог с контекстом

```
💬 Вы: Расскажи про Go

🤖 Агент: Go (или Golang) - это современный язык программирования, 
разработанный в Google. Основные особенности:
- Статическая типизация
- Встроенная поддержка конкурентности (goroutines)
- Быстрая компиляция
- Простой синтаксис

💬 Вы: А какие у него недостатки?

🤖 Агент: У Go есть несколько спорных моментов:
- Отсутствие дженериков (до версии 1.18)
- Многословная обработка ошибок
- Нет перегрузки функций
- Ограниченные возможности метапрограммирования

Однако многие считают эти ограничения преимуществами, 
способствующими написанию простого и понятного кода.
```

Обратите внимание: **агент помнит контекст** и понимает, что "у него" = "у Go".

#### Пример 3: Использование команд

```
💬 Вы: /history

📜 ИСТОРИЯ ДИАЛОГА
────────────────────────────────────────────────────────

💬 Вы [14:23:15]:
Расскажи про Go

🤖 Агент [14:23:17]:
Go (или Golang) - это современный язык программирования...

────────────────────────────────────────────────────────

💬 Вы [14:23:45]:
А какие у него недостатки?

🤖 Агент [14:23:47]:
У Go есть несколько спорных моментов...

💬 Вы: /stats

📊 СТАТИСТИКА
────────────────────────────────────────────────────────

Запросов выполнено: 2
Сообщений в истории: 4
Токенов использовано: 245
Токенов в памяти (оценка): 180
Среднее токенов/запрос: 122.5
Примерная стоимость: $0.000098

💬 Вы: /clear

✓ История диалога очищена

💬 Вы: /exit

До свидания! 👋
```

## API агента

### Создание агента

```go
import "github.com/georgijter-grigoranc/ai-advent-challenge/internal/agent"

agentConfig := agent.AgentConfig{
    APIKey:       "your-api-key",
    Model:        "gpt-4o-mini",
    Temperature:  0.7,
    MaxTokens:    500,
    SystemPrompt: "Ты - полезный ассистент",
}

aiAgent := agent.NewAgent(agentConfig)
```

### Отправка запроса

```go
response, err := aiAgent.Ask("Привет!")
if err != nil {
    log.Fatal(err)
}

fmt.Println(response.Content)
fmt.Printf("Токены: %d\n", response.TokensUsed)
fmt.Printf("Время: %s\n", response.ExecutionTime)
```

### Работа с историей

```go
// Получить всю историю
history := aiAgent.GetHistory()
for _, msg := range history {
    fmt.Printf("%s: %s\n", msg.Role, msg.Content)
}

// Очистить историю
aiAgent.ClearHistory()

// Получить размер истории
size := aiAgent.GetHistorySize()

// Получить последнее сообщение ассистента
lastMsg := aiAgent.GetLastMessage()
```

### Изменение конфигурации

```go
// Изменить системный промпт
aiAgent.SetSystemPrompt("Ты - эксперт по Go")

// Получить оценку токенов в памяти
tokens := aiAgent.GetTotalTokens()
```

## Структура агента

### AgentConfig

```go
type AgentConfig struct {
    APIKey       string   // API ключ OpenAI
    Model        string   // Модель (gpt-4o-mini, gpt-4o, etc)
    Temperature  float32  // Температура (0.0-2.0)
    MaxTokens    int      // Максимум токенов в ответе
    SystemPrompt string   // Системный промпт
}
```

### Message

```go
type Message struct {
    Role      string    // "user", "assistant", "system"
    Content   string    // Текст сообщения
    Timestamp time.Time // Время создания
}
```

### Response

```go
type Response struct {
    Content          string        // Ответ агента
    TokensUsed       int           // Всего токенов
    PromptTokens     int           // Токенов в промпте
    CompletionTokens int           // Токенов в ответе
    ExecutionTime    time.Duration // Время выполнения
    Model            string        // Использованная модель
}
```

## Ключевые возможности

### 1. Память диалога

Агент автоматически сохраняет всю историю разговора:
- Сообщения пользователя
- Ответы ассистента
- Временные метки

Это позволяет вести длительный диалог с сохранением контекста.

### 2. Системный промпт

Вы можете настроить поведение агента через системный промпт:

```go
// Агент-эксперт
SystemPrompt: "Ты - эксперт по программированию на Go"

// Агент-помощник
SystemPrompt: "Ты - дружелюбный помощник, отвечай кратко"

// Агент-преподаватель
SystemPrompt: "Ты - преподаватель. Объясняй сложные вещи просто"
```

### 3. Управление историей

```go
// Очистка истории для нового диалога
aiAgent.ClearHistory()

// Проверка размера истории
if aiAgent.GetHistorySize() > 20 {
    // Слишком большая история, можно очистить
}

// Оценка токенов в памяти
tokens := aiAgent.GetTotalTokens()
if tokens > 4000 {
    // Приближаемся к лимиту контекста
}
```

### 4. Метаданные ответов

Каждый ответ содержит метаданные:
- Количество использованных токенов
- Время выполнения
- Модель, которая ответила

Это полезно для:
- Мониторинга производительности
- Отслеживания затрат
- Отладки

## Расширение агента

### Добавление новых возможностей

Агент легко расширяется. Примеры:

**1. Сохранение диалогов в файл**

```go
func (a *Agent) SaveHistory(filename string) error {
    data, err := json.Marshal(a.history)
    if err != nil {
        return err
    }
    return os.WriteFile(filename, data, 0644)
}
```

**2. Загрузка сохраненных диалогов**

```go
func (a *Agent) LoadHistory(filename string) error {
    data, err := os.ReadFile(filename)
    if err != nil {
        return err
    }
    return json.Unmarshal(data, &a.history)
}
```

**3. Ограничение размера истории**

```go
func (a *Agent) TrimHistory(maxMessages int) {
    if len(a.history) > maxMessages {
        // Сохраняем только последние N сообщений
        a.history = a.history[len(a.history)-maxMessages:]
    }
}
```

**4. Потоковая передача ответов (streaming)**

```go
func (a *Agent) AskStream(userMessage string, callback func(string)) error {
    // Реализация streaming API
    // Вызов callback для каждого полученного токена
}
```

## Преимущества такой архитектуры

### 1. Инкапсуляция

Вся сложность работы с API скрыта внутри агента:
- CLI интерфейс не знает про OpenAI API
- Легко заменить провайдера (OpenAI → Anthropic)
- Простое тестирование (можно мокировать агента)

### 2. Переиспользование

Агент можно использовать в разных контекстах:
- CLI приложение (как сейчас)
- Web API (добавить HTTP handlers)
- Telegram бот
- Discord бот
- GUI приложение

### 3. Масштабируемость

Легко добавить новые возможности:
- Множественные агенты (специалисты)
- Персистентность (сохранение в БД)
- Аналитика и логирование
- Rate limiting
- Кэширование ответов

### 4. Тестируемость

```go
// Можно создать mock агента для тестов
type MockAgent struct {
    responses []string
}

func (m *MockAgent) Ask(msg string) (*Response, error) {
    // Возвращаем заготовленный ответ
    return &Response{Content: m.responses[0]}, nil
}
```

## Примеры use cases

### 1. Персональный ассистент

```go
agent := agent.NewAgent(agent.AgentConfig{
    SystemPrompt: `Ты - персональный ассистент пользователя.
                   Запоминай информацию о пользователе и его предпочтениях.`,
})
```

### 2. Помощник программиста

```go
agent := agent.NewAgent(agent.AgentConfig{
    SystemPrompt: `Ты - эксперт по программированию.
                   Помогай с кодом, объясняй концепции, находи баги.`,
})
```

### 3. Обучающий бот

```go
agent := agent.NewAgent(agent.AgentConfig{
    SystemPrompt: `Ты - преподаватель.
                   Объясняй сложные темы простым языком.
                   Задавай вопросы для проверки понимания.`,
})
```

## Ограничения и будущие улучшения

### Текущие ограничения

1. **Размер контекста**: История растет без ограничений
   - Решение: добавить автоматическую обрезку старых сообщений

2. **Нет персистентности**: История теряется при выходе
   - Решение: сохранение в файл или базу данных

3. **Однопоточность**: Один запрос в момент времени
   - Решение: добавить поддержку concurrent запросов

4. **Нет обработки ошибок API**: Простая обработка ошибок
   - Решение: retry logic, exponential backoff

### Возможные улучшения

**1. Умное управление контекстом**
```go
// Автоматически обрезать историю при приближении к лимиту
// Суммировать старые сообщения
// Сохранять только важную информацию
```

**2. Множественные агенты**
```go
// Создать специализированных агентов
codeAgent := NewAgent(codeConfig)
chatAgent := NewAgent(chatConfig)
// Роутить запросы к нужному агенту
```

**3. RAG (Retrieval Augmented Generation)**
```go
// Добавить базу знаний
// Искать релевантную информацию
// Включать в контекст агента
```

**4. Function calling**
```go
// Агент может вызывать функции
// Например: поиск в интернете, калькулятор, API запросы
```

## Заключение

Этот агент - это **первый шаг** в мире AI агентов. Он демонстрирует:

✅ Как правильно инкапсулировать логику работы с LLM  
✅ Как управлять состоянием (память диалога)  
✅ Как создать удобный интерфейс взаимодействия  
✅ Как сделать архитектуру расширяемой  

Отсюда можно развиваться в сторону:
- **Multi-agent systems** (несколько агентов работают вместе)
- **Autonomous agents** (агенты выполняют задачи самостоятельно)
- **Tool-using agents** (агенты используют внешние инструменты)

Это фундамент для более сложных AI систем! 🚀
