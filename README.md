# LLM CLI Client

Минимальная программа для работы с LLM через API с поддержкой разных провайдеров.

## Возможности

- ✅ Интерфейс `Provider` для подключения разных LLM провайдеров
- ✅ Реализация для OpenAI API
- ✅ CLI интерфейс для интерактивной работы
- ✅ Обработка ошибок

## Установка

```bash
# Клонируйте репозиторий
git clone <your-repo>
cd AI-Advent-Challenge

# Инициализируйте модуль (если нужно)
go mod init llm-client
```

## Использование

### Настройка

Установите переменную окружения с API ключом OpenAI:

```bash
export OPENAI_API_KEY="your-api-key-here"
```

### Запуск

```bash
go run main.go
```

### Пример работы

```
🤖 LLM CLI Client (OpenAI)
Введите ваш запрос (или 'exit' для выхода):

👉 Вы: Привет! Расскажи о Go языке программирования

⏳ Отправка запроса...

🤖 Ответ:
Go (или Golang) — это статически типизированный компилируемый язык программирования...

👉 Вы: exit
Пока! 👋
```

## Архитектура

### Интерфейс Provider

```go
type Provider interface {
    SendRequest(prompt string) (string, error)
}
```

Этот интерфейс позволяет легко добавлять новых провайдеров (Anthropic, Google, Ollama и т.д.)

### Добавление нового провайдера

Создайте структуру, реализующую интерфейс `Provider`:

```go
type AnthropicProvider struct {
    APIKey string
}

func (p *AnthropicProvider) SendRequest(prompt string) (string, error) {
    // Реализация для Anthropic API
    return "", nil
}
```

## Настройка

Вы можете изменить модель в коде:

```go
provider := &OpenAIProvider{
    APIKey: apiKey,
    Model:  "gpt-4", // или "gpt-3.5-turbo"
}
```

## Требования

- Go 1.21 или выше
- API ключ OpenAI
