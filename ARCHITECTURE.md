# Архитектура проекта AI Advent Challenge

## 📁 Структура проекта

```
AI-Advent-Challenge/
├── cmd/advent/              # Точки входа приложения
│   ├── day1/main.go        # День 1: Первый запрос к API
│   └── day2/main.go        # День 2: Контроль формата ответов
├── internal/               # Внутренние пакеты
│   ├── client/            # OpenAI клиент
│   │   └── openai.go     # Обертка над API
│   └── config/           # Конфигурация
│       └── config.go     # Загрузка .env и валидация
├── pkg/                   # Публичные утилиты
│   └── utils/
│       └── printer.go    # Утилиты для красивого вывода
├── .env                   # Переменные окружения (не в git)
├── .gitignore            # Исключения для git
├── Makefile              # Команды для сборки и запуска
├── README.md             # Документация проекта
├── go.mod                # Go модули
└── go.sum                # Хеши зависимостей
```

## 🏛️ Принципы архитектуры

### 1. Standard Go Project Layout

Проект следует стандартной структуре Go проектов:

- **cmd/** - точки входа (main packages)
- **internal/** - приватные пакеты, недоступные для импорта извне
- **pkg/** - публичные библиотеки, которые можно переиспользовать

### 2. Separation of Concerns

Каждый пакет отвечает за свою область:

- **cmd/advent/dayN/** - только инициализация и запуск
- **internal/client/** - работа с OpenAI API
- **internal/config/** - управление конфигурацией
- **pkg/utils/** - вспомогательные функции

### 3. Dependency Injection

```go
// Конфигурация создается один раз
cfg, _ := config.Load()

// Клиент инициализируется с конфигом
aiClient := client.NewOpenAIClient(cfg.OpenAIKey)

// Клиент передается дальше для использования
resp, _ := aiClient.CreateCompletion(...)
```

### 4. Error Handling

Все функции возвращают ошибки:

```go
func Load() (*Config, error) {
    if apiKey == "" {
        return nil, fmt.Errorf("API key not found")
    }
    return &Config{OpenAIKey: apiKey}, nil
}
```

## 🔄 Поток данных

```
┌─────────────┐
│   cmd/      │  1. Загружает конфигурацию
│  main.go    │  2. Создает клиент
└──────┬──────┘  3. Вызывает API
       │         4. Выводит результат
       ↓
┌─────────────┐
│  internal/  │  - Управляет запросами
│   client/   │  - Формирует ответы
│  openai.go  │  - Обрабатывает ошибки
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ OpenAI API  │  - GPT-4o-mini
│             │  - REST API
└─────────────┘
```

## 📦 Пакеты и их роли

### internal/config

**Назначение:** Управление конфигурацией приложения

**Функции:**
- `Load()` - загружает .env и возвращает Config
- Валидирует наличие обязательных переменных

**Использование:**
```go
cfg, err := config.Load()
if err != nil {
    log.Fatal(err)
}
```

### internal/client

**Назначение:** Обертка над OpenAI API

**Структуры:**
- `OpenAIClient` - основной клиент
- `CompletionRequest` - параметры запроса
- `CompletionResponse` - структурированный ответ

**Функции:**
- `NewOpenAIClient(apiKey)` - создание клиента
- `CreateCompletion(req)` - выполнение запроса

**Преимущества:**
- Упрощенный интерфейс
- Типобезопасность
- Единообразная обработка ошибок
- Легкое тестирование

**Использование:**
```go
client := client.NewOpenAIClient(apiKey)
resp, err := client.CreateCompletion(client.CompletionRequest{
    Prompt: "Hello",
    MaxTokens: 100,
    Temperature: 0.7,
})
```

### pkg/utils

**Назначение:** Утилиты для красивого вывода

**Функции:**
- `PrintHeader()` - заголовки
- `PrintSection()` - секции с эмодзи
- `PrintSuccess()` - успешные сообщения (зеленые)
- `PrintError()` - ошибки (красные)
- `PrintTokenStats()` - статистика токенов

**Использование:**
```go
utils.PrintHeader("My App")
utils.PrintSuccess("Done!")
utils.PrintTokenStats(100, 20, 80)
```

## 🚀 Расширяемость

### Добавление нового дня

1. Создать директорию:
```bash
mkdir -p cmd/advent/day3
```

2. Создать main.go:
```go
package main

import (
    "github.com/georgijter-grigoranc/ai-advent-challenge/internal/client"
    "github.com/georgijter-grigoranc/ai-advent-challenge/internal/config"
)

func main() {
    cfg, _ := config.Load()
    aiClient := client.NewOpenAIClient(cfg.OpenAIKey)
    
    // Ваша логика
}
```

3. Добавить в Makefile:
```makefile
day3: ## Запустить Day 3
	@go run cmd/advent/day3/main.go
```

### Добавление нового провайдера (Anthropic, Gemini)

1. Создать интерфейс в `internal/client/`:
```go
type AIClient interface {
    CreateCompletion(req CompletionRequest) (*CompletionResponse, error)
}
```

2. Реализовать для каждого провайдера:
```go
type AnthropicClient struct { ... }
func (c *AnthropicClient) CreateCompletion(...) { ... }
```

## 🎯 Лучшие практики

### 1. Не дублируйте код
Если логика повторяется - вынесите в функцию в pkg/utils/

### 2. Обрабатывайте ошибки
```go
if err != nil {
    log.Fatalf("Критическая ошибка: %v", err)
}
```

### 3. Используйте константы
```go
const (
    DefaultModel = openai.GPT4oMini
    DefaultTemperature = 0.7
)
```

### 4. Документируйте публичные функции
```go
// Load загружает конфигурацию из .env файла
// Возвращает ошибку, если OPENAI_API_KEY не установлен
func Load() (*Config, error) { ... }
```

## 🧪 Тестирование (TODO)

Структура для будущих тестов:

```
internal/
├── client/
│   ├── openai.go
│   └── openai_test.go
├── config/
│   ├── config.go
│   └── config_test.go
```

Пример теста:
```go
func TestLoad(t *testing.T) {
    os.Setenv("OPENAI_API_KEY", "test-key")
    cfg, err := Load()
    if err != nil {
        t.Errorf("Expected no error, got %v", err)
    }
}
```

## 📝 Соглашения по коду

### Именование
- Пакеты: `lowercase` без underscore
- Файлы: `lowercase` с underscore при необходимости
- Типы: `PascalCase`
- Функции: `PascalCase` (публичные), `camelCase` (приватные)
- Переменные: `camelCase`

### Импорты
```go
import (
    // Стандартная библиотека
    "fmt"
    "log"
    
    // Внешние зависимости
    "github.com/joho/godotenv"
    
    // Внутренние пакеты
    "github.com/georgijter-grigoranc/ai-advent-challenge/internal/client"
)
```

## 🔐 Безопасность

- API ключи только в `.env`
- `.env` в `.gitignore`
- Никогда не хардкодить секреты
- Использовать переменные окружения

## 📚 Дополнительные ресурсы

- [Standard Go Project Layout](https://github.com/golang-standards/project-layout)
- [Effective Go](https://golang.org/doc/effective_go)
- [OpenAI API Documentation](https://platform.openai.com/docs)
